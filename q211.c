#include<stdio.h>
int main()
{
    int number,digit;
    printf("Enter a number:");
    scanf("%d",&number);
    printf("Enter a digit:");
    scanf("%d",&digit);
    printf("The new number is %d%d",number,digit);
    return 0;
}